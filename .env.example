# Copy this file to .env.local and fill in your values. Never commit .env.local to git.

# --- OpenAI ---
OPENAI_API_KEY=sk-...
# Optional: override the default models used by llm_client.py
OPENAI_DEFAULT_MODEL=gpt-5.4-mini
OPENAI_FALLBACK_MODEL=gpt-5.5
# Optional: total token ceiling the Token usage page tracks against.
# Purely for monitoring — nothing enforces it against the provider account.
# Leave unset for no limit.
# TOKEN_BUDGET=500000

# --- MySQL (required by docker-compose db service) ---
MYSQL_HOST=db
MYSQL_PORT=3306
MYSQL_DATABASE=adblock
MYSQL_USER=adblock
MYSQL_PASSWORD=adblock_pass

# Ceph / S3-compatible storage
S3_ENABLED=false
S3_ACCESS_KEY=
S3_SECRET_KEY=
AWS_ENDPOINT=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
S3_VERIFY_SSL=true
AWS_REQUEST_CHECKSUM_CALCULATION=when_required
AWS_RESPONSE_CHECKSUM_VALIDATION=when_required